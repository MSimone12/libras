import detected from "./assets/detectada.png";
import notDetected from "./assets/nao_detectada.png";
import deactivated from "./assets/desativada.png";

const hands = {
  DETECTED: detected,
  NOT_DETECTED: notDetected,
  DEACTIVATED: deactivated,
};

export default hands;
